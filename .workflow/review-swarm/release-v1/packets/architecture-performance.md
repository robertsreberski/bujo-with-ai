# Architecture and performance packet

Review package boundaries and runtime composition for coupling, dead code,
unbounded hot paths, leaks, invalid lifecycle ordering, startup/build portability,
and risks to the documented capture/MCP latency targets.

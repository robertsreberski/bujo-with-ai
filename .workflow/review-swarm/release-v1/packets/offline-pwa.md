# Offline and PWA packet

Review `app/src/store`, `app/src/pwa`, `app/src/sw.ts`, and their tests for
offline correctness, outbox idempotency, SSE convergence, replay/reset behavior,
IndexedDB durability, update lifecycle, and iOS/PWA regressions.

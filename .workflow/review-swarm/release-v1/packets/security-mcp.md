# Security and MCP packet

Review server HTTP, SSE, MCP, configuration, and adapters for auth bypasses,
Host/Origin/CSRF defects, token leakage, prompt-injection boundary failures,
session/revocation defects, rate-limit bypasses, unsafe egress, and contract
drift from the exact seven-tool surface.

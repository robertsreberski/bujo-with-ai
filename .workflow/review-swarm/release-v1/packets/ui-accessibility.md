# UI and accessibility packet

Review `app/src/components`, `app/src/views`, `app/src/styles`, route behavior,
and browser tests against SPEC-03 through SPEC-05, with emphasis on semantic
controls, focus, dialogs, live regions, touch targets, responsive layouts, local
dates, and honest failure states.
